
const state = {
  settingConsoleData: {}
};

const getters = {};

const actions = {};

const mutations = {
  setSettingConsoleData(state, settingConsoleData) {
    state.settingConsoleData = settingConsoleData;
  }
};

export default {
  namespaces: true,
  state,
  getters,
  actions,
  mutations
};
